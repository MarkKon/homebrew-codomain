# Homebrew Codomain

Custom Homebrew tap for Codomain.

## Install

```sh
brew tap MarkKon/codomain
brew install --cask codomain
```

## Update

```sh
brew update
brew upgrade --cask codomain
```

The cask is updated automatically by the `MarkKon/codomain` release workflow when a `v*` tag is published.
